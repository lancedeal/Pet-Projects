var BasicDeck = [
    {
        head:'Noun',
        body:'a person,place, or thing'
    },
    {
        head:'Verb',
        body:'action words'
    },
    {
        head:'Adjective',
        body:'descriptive language'
    }
]
var ExpressDeck = [
    {
        head:'express.static(root, [options])',
        body:'This is a built-in middleware function in Express. It serves static files and is based on serve-static. The root argument specifies the root directory from which to serve static assets.'
    },
    {
        head:'express.urlencoded([options])',
        body:'Returns middleware that only parses urlencoded bodies and only looks at requests where the Content-Type header matches the type option.'
    },
    {
        head:'express.json([options])',
        body:'Returns middleware that only parses JSON and only looks at requests where the Content-Type header matches the type option.'
    },
    {
        head:'express.raw([options])',
        body:'Returns middleware that parses all bodies as a Buffer and only looks at requests where the Content-Type header matches the type option.'
    },
    {
        head:'express.Router([options])',
        body:'Creates a new router object.'
    },
    {
        head:'express.text([options])',
        body:'Returns middleware that parses all bodies as a string and only looks at requests where the Content-Type header matches the type option.'
    },
    {
        head:'app.render(view, [locals], callback)',
        body:'Returns the rendered HTML of a view via the callback function. It accepts an optional parameter that is an object containing local variables for the view. It is like res.render(), except it cannot send the rendered view to the client on its own.'
    },
    {
        head:'res.sendFile(path [, options] [, fn])',
        body:'Transfers the file at the given path. Sets the Content-Type response HTTP header field based on the filename’s extension. Unless the root option is set in the options object, path must be an absolute path to the file.'
    },
    {
        head:'res.render(view [, locals] [, callback])',
        body:'Renders a view and sends the rendered HTML string to the client.'
    },
    {
        head:'res.type(type)',
        body:'Sets the Content-Type HTTP header to the MIME type as determined by the specified type.'
    }
]

var NodeDeck = [
    {
        head:'emitter.emit(eventName[, ...args])',
        body:'Synchronously calls each of the listeners registered for the event named eventName, in the order they were registered, passing the supplied arguments to each.'
    },
    {
        head:'emitter.on(eventName, listener)',
        body:'Adds the listener function to the end of the listeners array for the event named eventName'
    },
    {
        head:'emitter.once(eventName, listener)',
        body:'Adds a one-time listener function for the event named eventName. The next time eventName is triggered, this listener is removed and then invoked.'
    },
    {
        head:'emitter.prependListener(eventName, listener)',
        body:'Adds the listener function to the beginning of the listeners array for the event named eventName'
    },
    {
        head:'emitter.prependOnceListener(eventName, listener)',
        body:'Adds a one-time listener function for the event named eventName to the beginning of the listeners array.'
    },
    {
        head:'emitter.removeAllListeners([eventName])',
        body:'Removes all listeners, or those of the specified eventName'
    },
    {
        head:'emitter.removeListener(eventName, listener)',
        body:'Removes the specified listener from the listener array for the event named eventName.'
    },
    {
        head:'emitter.rawListeners(eventName)',
        body:'Returns a copy of the array of listeners for the event named eventName, including any wrappers (such as those created by .once())'
    },
]

var JsDeck = [
    {
        head:'String.prototype.length()',
        body:'Reflects the length of the string'
    },
    {
        head:'String.prototype.at(index)',
        body:'Returns the character (exactly one UTF-16 code unit) at the specified index. Accepts negative integers, which count back from the last string character.'
    },
    {
        head:'String.prototype.concat(str [, ...strN ])',
        body:'Combines the text of two (or more) strings and returns a new string.'
    },
    {
        head:'String.prototype.includes(searchString [, position])',
        body:'Determines whether the calling string contains searchString.'
    },
    {
        head:'String.prototype.match(regexp)',
        body:'Used to match regular expression regexp against a string.'
    },
    {
        head:'String.prototype.repeat(count)',
        body:'Returns a string consisting of the elements of the object repeated count times.'
    },
    {
        head:'String.prototype.replace(searchFor, replaceWith)',
        body:'Used to replace occurrences of searchFor using replaceWith. searchFor may be a string or Regular Expression, and replaceWith may be a string or function.'
    },
    {
        head:'String.prototype.slice(beginIndex[, endIndex])',
        body:'Extracts a section of a string and returns a new string.'
    },
]

var template = [
    {
        head:'',
        body:''
    },
    {
        head:'',
        body:''
    },
    {
        head:'',
        body:''
    },
    {
        head:'',
        body:''
    },
    {
        head:'',
        body:''
    },
    {
        head:'',
        body:''
    },
    {
        head:'',
        body:''
    },
    {
        head:'',
        body:''
    },
    {
        head:'',
        body:''
    },
    {
        head:'',
        body:''
    }
]